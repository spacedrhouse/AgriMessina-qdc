# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# --- Venv ---
# Attualmente qdc/ è una mixed venv+sources dir: queste path sono
# l'output di `python -m venv`. Non vanno committate.
bin/
include/
lib/
lib64/
pyvenv.cfg
.venv/
venv/

# --- PyInstaller output ---
build/
dist/

# --- Inno Setup output ---
installer/output/

# --- Editors / IDE ---
.vscode/
.idea/
*.swp
*.swo

# --- Claude Code (per-utente, non da committare) ---
.claude/
settings.local.json

# --- Logs ---
*.log

# --- DB locali / cache utente ---
*.db
*.db-shm
*.db-wal
.agrimessina/

# --- Excel exports di test (sample) ---
Movimenti_Magazzino.xlsx
Trattamenti_Revisionati.xlsx

# --- Legacy / dipendenze native non più necessarie ---
libmariadb.dll

# --- Secret iniettato a build-time (token GitHub per auto-update privato) ---
# Generato dal workflow CI da `secrets.RELEASE_READ_PAT`; in dev, opzionale,
# crealo a mano se vuoi testare l'auto-update localmente. MAI committare.
_secrets.py
